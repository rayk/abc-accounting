name: abc_accounting
description: >-
  The Ledger interface: token-guarded Ledger contract, vocabulary types,
  and contractVersion. No implementation; consumable directly from pub.dev or a git tag.
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"

resolution: workspace

dependencies:
  fpdart: ^1.2.0

dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^10.2.0
