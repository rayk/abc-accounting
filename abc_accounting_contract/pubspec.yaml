name: abc_accounting_contract
description: >-
  Dev-only conformance kit for abc_accounting. Never released.
version: 0.1.0
publish_to: none

environment:
  # Bumped to >=3.8.0 for parameterized_test 2.0.3 (Phase 6 tables).
  sdk: ">=3.8.0 <4.0.0"

resolution: workspace

dependencies:
  abc_accounting:
    path: ../abc_accounting
  checks: ^0.3.1
  fpdart: ^1.2.0
  test: ^1.25.0
  bnd_eac:
    git:
      url: https://github.com/rayk/bounded.git
      ref: 'c9de583ec85019807766b6327774b823900a26f4'
      path: packages/bnd-eac

dev_dependencies:
  # Phase 6 negative-case tables (CONTRACT_AUTHORING_SPEC §step 6).
  parameterized_test: ^2.0.3
  very_good_analysis: ^10.2.0
